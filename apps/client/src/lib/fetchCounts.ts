// import axios from "axios";
// import { api_endpoint } from "./apiEndPoint";
// import { setError } from "./error";

// export const fetchCounts = async (setCount) => {
//   try {
//     const countResponse = await axios.get(`${api_endpoint}/counts`);
//     setCount(countResponse.data);
//     console.log("Counts Response:", countResponse.data);
//   } catch (error) {
//     setError("Error fetching counts");
//   }
// };
